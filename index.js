const calculate = require('./src/calculate')
const validate = require('./src/validate')
const pickAPrimary = require('./src/pickAPrimary')

module.exports = {
  calculate,
  validate,
  pickAPrimary,
}
